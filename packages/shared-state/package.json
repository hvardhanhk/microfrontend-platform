{
  "name": "@platform/shared-state",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src/",
    "test": "jest --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@platform/types": "*",
    "@platform/event-bus": "*",
    "zustand": "^5.0.2"
  }
}
